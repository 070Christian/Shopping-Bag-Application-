//Given Retail interface 

public interface Retail {
	  double RetailCost();            // total cost of all items before tax

	    double AverageCost();        // average cost of an item, before tax

	    double TotalCost();             // total cost of all items including tax

	    int TotalItems();                  // number of items in the bag

		
}
